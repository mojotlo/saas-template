// Tailwind v4 — theme configuration is handled via CSS @theme in globals.css.
// This file exists for any v4-compatible overrides needed at build time.
import type { Config } from 'tailwindcss'

const config: Config = {
  darkMode: 'class',
}

export default config
