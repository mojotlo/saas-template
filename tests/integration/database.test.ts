import { describe, it, expect } from 'vitest'

// Placeholder integration test — confirms the database connection works.
// Replace this with real integration tests as you build out your data layer.
//
// Integration tests run against a real database.
// Locally: docker compose up -d before running npm run test:integration
// CI: Docker Postgres spins up automatically via GitHub Actions services

describe('database connection', () => {
  it('placeholder — add real integration tests here', () => {
    // This test exists so Vitest has something to run.
    // Delete it once you have real integration tests.
    expect(true).toBe(true)
  })
})
